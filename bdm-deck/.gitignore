# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local backups (e.g. *.svg.bak from asset iteration)
*.bak

# Playwright runtime output — screenshots + health dumps are regenerated
# on every run, never hand-curated, not client-facing.
test-results/
playwright-report/
tests/screenshots/

# Service account credentials — never commit
server/*-key.json
*.credentials.json
.env
.env.*

# Script preview/error outputs (timestamps; regenerated on every run)
scripts/out/
